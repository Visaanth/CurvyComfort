PORT=5000

# Database Connection Configuration
# For local MySQL setup, customize these values:
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=curvy_comfort

# Alternatively, define the full connection string directly:
# DATABASE_URL=mysql://username:password@host:port/database

# Admin Login Credentials (Optional, falls back to defaults)
# ADMIN_EMAIL=admin@curvycomfort@gmail.com
# ADMIN_PASSWORD=CurComf2026
